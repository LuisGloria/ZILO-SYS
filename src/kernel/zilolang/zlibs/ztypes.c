#include "ztypes.h"

/*
 * ZILO TYPE SYSTEM RUNTIME (currently empty)
 *
 * This file exists for:
 * - future ABI compatibility layer
 * - runtime type introspection (optional Evo[] upgrade)
 * - forcing linkage consistency in freestanding builds
 *
 * At the moment: intentionally no code.
 */
